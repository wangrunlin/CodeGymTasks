taskKey="zh.codegym.task.task13.task1309"\n\n所有可移动之物

1.创建一个有 speed 方法的 CanMove 接口。
2.该 speed 方法必须返回一个 Double，不应该接受任何参数。
3.让 CanFly 接口继承 CanMove 接口。
4.将 speed 方法添加到 CanFly 接口。
5.该 speed 方法必须返回一个 Double，接受一个 CanFly 参数。


Requirements:
1.	Solution 类必须声明一个 CanMove 接口。
2.	Solution 类必须声明一个 CanFly 接口。
3.	CanFly 接口必须继承 CanMove 接口。
4.	CanMove 接口必须声明一个没有参数的 speed 方法，并且必须返回 Double。
5.	CanFly 接口必须声明具有一个 CanFly 参数的 speed 方法，并且必须返回 Double。


